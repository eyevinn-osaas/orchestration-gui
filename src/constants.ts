/**
 * The API version of the Agile Live API that is currently supported.
 * @constant
 */
export const AGILE_API_VERSION = 'v3';

/**
 * The base path for the Agile Live API, where all other endpoints can be appended.
 * @constant
 */
export const AGILE_BASE_API_PATH = '/api/' + AGILE_API_VERSION;
