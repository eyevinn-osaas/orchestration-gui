/**
 * The API version of the Ateliere Live API that is currently supported.
 * @constant
 */
export const LIVE_API_VERSION = 'v3';

/**
 * The base path for the Ateliere Live API, where all other endpoints can be appended.
 * @constant
 */
export const LIVE_BASE_API_PATH = '/api/' + LIVE_API_VERSION;
